### Fixed

- The embedded terminal now follows theme switches. Previously the terminal kept its original background and text colors after changing the theme, while the rest of the interface updated correctly. ([#18](https://github.com/ttnear/Clarc/issues/18))

### Documentation

- The README now documents Homebrew installation: `brew install --cask clarc`.
